import statistics
notes = [0, 7, 8, 15, 16, 18, 5]
'''
def moyenne(tab):
    add = 0
    for i in range(len(tab)):
        add += tab[i]
    
    result = add / len(tab)
    return result

moyenne(notes)


moy = statistics.mean(notes)
if moy == moyenne(notes):
    print(moyenne(notes))

else:
    print('erreur')

    '''
